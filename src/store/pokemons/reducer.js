const initialState = {
  list: [],
  fetching: false,
}

const pokemonsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default pokemonsReducer;
